" I have a very idiosyncratic style in TeX/LaTex wherein each grammatical
" clause is its own line in the source code file. Sometimes this mean full
" sentences, but usually, it means lines are split on punctuation. That way,
" it's trivially possible to rearrange clauses in a sentence or paragraph:
setlocal textwidth=0
